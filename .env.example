# ==============================================================================
# INFOMUNDI LOCAL DEVELOPMENT ENVIRONMENT
# ==============================================================================
# Copy this file to .env and customize for your local development environment
#
# Quick start:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up -d
# ==============================================================================

# ------------------------------------------------------------------------------
# Flask Configuration
# ------------------------------------------------------------------------------
SESSION_COOKIE_NAME=infomundi_dev
BASE_URL=http://localhost  # without the trailing slash, so http://localhost/ would be WRONG
NEWS_SEARCH_DEBUG=1  # debug on means it'll search news only for the br_general category

# Generate secure keys with: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
ENCRYPTION_KEY=your-encryption-key-here-generate-with-openssl-rand-hex-32
HMAC_KEY=your-hmac-key-here-generate-with-openssl-rand-hex-32

# ------------------------------------------------------------------------------
# Database (MySQL)
# ------------------------------------------------------------------------------
# These are used by Docker Compose to create the database
# Use simple passwords for local dev - this is NOT production!
MYSQL_ROOT_PASSWORD=dev_root_pass
MYSQL_DATABASE=infomundi
MYSQL_USERNAME=infomundi
MYSQL_PASSWORD=dev_pass

# ------------------------------------------------------------------------------
# Cache (Redis)
# ------------------------------------------------------------------------------
REDIS_PASSWORD=dev_redis_pass

# ------------------------------------------------------------------------------
# Email (SMTP)
# ------------------------------------------------------------------------------
# For local dev, we use MailHog (included in docker-compose.yml)
# MailHog catches all emails - view them at http://localhost:8025
SMTP_SERVER=mailhog
SMTP_PORT=1025
SMTP_PASSWORD=

# To test with real SMTP (optional):
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_PASSWORD=your-app-password

# ------------------------------------------------------------------------------
# CAPTCHA (Cloudflare Turnstile)
# ------------------------------------------------------------------------------
# For local dev, use Cloudflare's test keys (these always pass)
# Get real keys at: https://dash.cloudflare.com/
TURNSTILE_SITE_KEY=1x00000000000000000000AA
TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA

# ------------------------------------------------------------------------------
# AI Features (Optional)
# ------------------------------------------------------------------------------
# Leave empty to disable AI features in development
# Get API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ------------------------------------------------------------------------------
# Storage (Cloudflare R2)
# ------------------------------------------------------------------------------
# Leave empty for local dev - file uploads won't work but app will still run
# Get credentials at: https://dash.cloudflare.com/
R2_ENDPOINT=
R2_ACCESS_KEY=
R2_SECRET=
R2_TOKEN=

# ------------------------------------------------------------------------------
# OAuth (Google) - Optional
# ------------------------------------------------------------------------------
# Leave empty to disable Google OAuth in development
# Get credentials at: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ------------------------------------------------------------------------------
# Custom CAPTCHA Service (Optional)
# ------------------------------------------------------------------------------
# Only needed if you're running the custom CAP service
# Uncomment the cap service in docker-compose.yml if using
CAP_HOSTNAME=cap
CAP_SITE_KEY=
CAP_SECRET_KEY=
CAP_ADMIN_KEY=local-dev-admin-key

# ------------------------------------------------------------------------------
# Monitoring & Alerts (Optional)
# ------------------------------------------------------------------------------
# Leave empty for local dev
# Webhook for error alerts (e.g., Discord webhook URL)
WEBHOOK_URL=

# ------------------------------------------------------------------------------
# Advanced Configuration (usually don't need to change)
# ------------------------------------------------------------------------------
WEBSITE_ROOT=/app
LOCAL_ROOT=/app
